
# QR 生成

- 事前に QR の png を生成します
- QR と _entries テーブルの relation のデータを作成します

`qr` ディレクトリを `qrarxv.tar.gz` に圧縮

```
tar zcvf qrarxv.tar.gz qr
```

`qrarxv.tar.gz` を展開。 `qr` ディレクトリが作られる

```
tar zxvf qrarxv.tar.gz
```
